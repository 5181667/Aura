# 🔄 替换现有项目部署指南

当您的阿里云服务器上已经有项目在运行时，使用此指南安全地替换为 AuraTest 项目。

## ⚠️ 重要提示

- ✅ 会自动备份现有项目
- ✅ 会保存现有的配置文件和上传文件
- ✅ 提供完整的回滚方案
- ✅ 支持保留现有数据库

## 🚀 快速替换（推荐）

### 方法一：使用替换脚本

```bash
# 1. 连接到服务器
ssh root@你的服务器IP

# 2. 下载替换脚本
curl -o replace-deploy.sh https://raw.githubusercontent.com/5181667/Aura/main/replace-deploy.sh

# 3. 添加执行权限
chmod +x replace-deploy.sh

# 4. 运行脚本（会有交互式提示）
sudo ./replace-deploy.sh
```

### 脚本会提示您输入：

1. **现有项目路径** - 例如：`/var/www/old-project`
2. **要停止的 PM2 应用名** - 例如：`old-app`
3. **新项目安装路径** - 默认：`/var/www/auratest`
4. **是否使用旧的环境配置**
5. **是否创建新数据库**（如果使用现有数据库选择 n）

### 脚本自动完成：

- ✅ 备份现有项目到 `/var/backups/auratest/backup_时间戳`
- ✅ 保存 `.env` 配置
- ✅ 保存 `public/uploads` 上传文件
- ✅ 停止旧应用
- ✅ 克隆新项目
- ✅ 安装依赖
- ✅ 配置环境
- ✅ 启动新应用

## 📋 方法二：手动替换步骤

### 1. 备份现有项目

```bash
# 创建备份目录
mkdir -p /var/backups/auratest

# 备份项目（替换为你的实际路径）
cp -r /var/www/your-old-project /var/backups/auratest/backup_$(date +%Y%m%d_%H%M%S)

# 备份配置文件
cp /var/www/your-old-project/.env /var/backups/auratest/env_backup_$(date +%Y%m%d_%H%M%S)

# 备份上传文件（如果有）
cp -r /var/www/your-old-project/public/uploads /var/backups/auratest/uploads_backup_$(date +%Y%m%d_%H%M%S)
```

### 2. 停止现有应用

```bash
# 查看运行中的 PM2 应用
pm2 list

# 停止旧应用（替换为实际应用名）
pm2 stop your-old-app
```

### 3. 克隆新项目

```bash
# 克隆到新目录
cd /var/www
git clone https://github.com/5181667/Aura.git auratest
cd auratest
```

### 4. 配置环境

**选项 A：使用旧配置**
```bash
# 复制旧的 .env 文件
cp /var/backups/auratest/env_backup_* .env

# 根据需要更新配置
nano .env
```

**选项 B：创建新配置**
```bash
# 创建 .env 文件
nano .env
```

填入：
```env
DATABASE_URL="postgresql://username:password@localhost:5432/dbname"
NEXTAUTH_SECRET="运行 openssl rand -base64 32 生成"
NEXTAUTH_URL="http://你的域名或IP"
NODE_ENV="production"
```

### 5. 恢复上传文件

```bash
# 恢复之前的上传文件
mkdir -p public/uploads
cp -r /var/backups/auratest/uploads_backup_*/* public/uploads/
```

### 6. 安装和构建

```bash
# 安装依赖
npm install

# 生成 Prisma Client
npx prisma generate

# 如果使用现有数据库，跳过此步骤
# 如果创建新数据库：
npx prisma db push

# 构建项目
npm run build
```

### 7. 更新 Nginx 配置

```bash
# 编辑 Nginx 配置
nano /etc/nginx/sites-available/auratest
```

```nginx
server {
    listen 80;
    server_name 你的域名或IP;

    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /socket.io/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

```bash
# 启用配置
ln -s /etc/nginx/sites-available/auratest /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

### 8. 启动新应用

```bash
# 删除旧的 PM2 应用
pm2 delete your-old-app

# 启动新应用
pm2 start npm --name "auratest" -- start

# 保存配置
pm2 save
```

## 🔙 回滚方案

如果新项目出现问题，可以快速回滚：

```bash
# 1. 停止新应用
pm2 stop auratest

# 2. 删除新项目
rm -rf /var/www/auratest

# 3. 恢复旧项目（替换为实际备份路径）
cp -r /var/backups/auratest/backup_20260117_120000 /var/www/auratest

# 4. 重启应用
cd /var/www/auratest
pm2 restart auratest
```

## 📊 数据库迁移选项

### 选项 A：使用现有数据库

如果你想保留现有数据库的数据：

```bash
# 只生成 Prisma Client，不修改数据库
npx prisma generate

# 如果需要添加新字段（会保留现有数据）
npx prisma db push
```

### 选项 B：创建新数据库

如果你想使用全新的数据库：

```bash
# 创建新数据库
sudo -u postgres psql
CREATE DATABASE auratest_new;
GRANT ALL PRIVILEGES ON DATABASE auratest_new TO your_user;
\q

# 更新 .env 中的 DATABASE_URL
# 然后运行迁移
npx prisma db push
node prisma/seed-disc.js
```

### 选项 C：迁移现有数据

如果需要将旧数据库的数据迁移到新项目：

```bash
# 1. 导出旧数据库
pg_dump old_database > /var/backups/old_db_backup.sql

# 2. 导入到新数据库
psql auratest < /var/backups/old_db_backup.sql

# 3. 运行 Prisma 迁移以添加新字段
npx prisma db push
```

## 🔍 验证部署

### 检查应用状态
```bash
pm2 status
pm2 logs auratest --lines 50
```

### 检查服务
```bash
# 检查 Nginx
systemctl status nginx

# 检查数据库
sudo -u postgres psql -d auratest -c "\dt"

# 检查端口
netstat -tunlp | grep 3000
```

### 访问测试
```bash
# 测试主页
curl http://localhost:3000

# 在浏览器中访问
http://你的服务器IP
```

## 🐛 常见问题

### 1. 端口冲突

如果 3000 端口被占用：

```bash
# 查看占用端口的进程
lsof -i :3000

# 终止进程（替换 PID）
kill -9 PID

# 或者修改 Next.js 端口
# 在项目中运行：
PORT=3001 pm2 start npm --name "auratest" -- start
```

### 2. 数据库连接失败

```bash
# 检查 PostgreSQL 状态
systemctl status postgresql

# 测试连接
psql -U your_user -d auratest -h localhost

# 检查 .env 中的 DATABASE_URL
```

### 3. PM2 进程未启动

```bash
# 查看错误日志
pm2 logs auratest --err --lines 100

# 手动测试启动
cd /var/www/auratest
npm start
```

### 4. Nginx 502 错误

```bash
# 检查应用是否运行
pm2 status

# 检查 Nginx 错误日志
tail -f /var/log/nginx/error.log

# 测试 Nginx 配置
nginx -t
```

## 📞 需要帮助？

- **查看日志**: `pm2 logs auratest --lines 200`
- **GitHub Issues**: https://github.com/5181667/Aura/issues
- **完整文档**: [ALIYUN_DEPLOYMENT.md](./ALIYUN_DEPLOYMENT.md)

## ✅ 部署检查清单

- [ ] 旧项目已备份
- [ ] 环境变量已配置
- [ ] 上传文件已恢复
- [ ] 数据库已配置
- [ ] Nginx 配置已更新
- [ ] 应用正常运行
- [ ] 可以通过浏览器访问
- [ ] 功能测试通过
- [ ] PM2 自启动已设置

---

**祝替换部署顺利！** 🎉

如有问题随时查看备份目录 `/var/backups/auratest/` 进行回滚。
