# 🚀 阿里云部署快速指南

## 📦 项目地址
GitHub: https://github.com/5181667/Aura.git

## ⚡ 快速部署（推荐）

### 第一步：连接服务器
```bash
ssh root@你的阿里云服务器IP
```

### 第二步：下载并运行部署脚本
```bash
# 下载脚本
curl -o deploy.sh https://raw.githubusercontent.com/5181667/Aura/main/deploy.sh

# 添加执行权限
chmod +x deploy.sh

# 运行部署脚本
sudo ./deploy.sh
```

脚本会自动：
- ✅ 安装所有依赖（Node.js, PostgreSQL, Nginx, PM2）
- ✅ 配置数据库
- ✅ 克隆项目
- ✅ 配置环境变量
- ✅ 构建并启动应用
- ✅ 配置 Nginx 反向代理
- ✅ 设置防火墙

### 第三步：访问应用
```
http://你的服务器IP
```

### 第四步：创建管理员账户
1. 在网页上注册一个账号
2. 在服务器上运行：
```bash
sudo -u postgres psql -d auratest
UPDATE "User" SET role = 'ADMIN' WHERE email = 'your-email@example.com';
\q
```

## 🔄 更新应用

当代码更新后，在服务器上运行：

```bash
cd /var/www/auratest
./update.sh
```

或手动更新：
```bash
cd /var/www/auratest
git pull origin main
npm install
npm run build
pm2 restart auratest
```

## 🌐 配置域名

### 1. 在阿里云购买域名并解析
- 添加 A 记录指向你的服务器 IP

### 2. 更新环境变量
```bash
nano /var/www/auratest/.env
```
修改：
```
NEXTAUTH_URL="https://你的域名.com"
```

### 3. 配置 SSL 证书
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d 你的域名.com
```

### 4. 重启应用
```bash
pm2 restart auratest
```

## 📊 常用命令

```bash
# 查看应用状态
pm2 status

# 查看日志
pm2 logs auratest

# 重启应用
pm2 restart auratest

# 停止应用
pm2 stop auratest

# 查看 Nginx 状态
sudo systemctl status nginx

# 查看数据库
sudo -u postgres psql -d auratest
```

## 🛡️ 安全建议

1. **修改 SSH 端口**（可选）
2. **禁用 root 登录**（可选）
3. **配置 SSL 证书**（强烈推荐）
4. **定期备份数据库**
5. **更新系统和依赖**

## 📝 阿里云安全组配置

在阿里云控制台 → ECS → 安全组，添加规则：

| 端口 | 协议 | 说明 |
|------|------|------|
| 22   | TCP  | SSH |
| 80   | TCP  | HTTP |
| 443  | TCP  | HTTPS |
| 3000 | TCP  | 开发环境（可选）|

## 🐛 故障排查

### 应用无法访问
```bash
# 检查应用状态
pm2 status
pm2 logs auratest

# 检查 Nginx
sudo nginx -t
sudo systemctl status nginx

# 检查端口
netstat -tunlp | grep 3000
```

### 数据库连接失败
```bash
# 检查 PostgreSQL
sudo systemctl status postgresql

# 测试连接
psql -U auratest_user -d auratest -h localhost
```

### 内存不足
```bash
# 添加 swap
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

## 📚 详细文档

查看完整部署文档：
- [ALIYUN_DEPLOYMENT.md](./ALIYUN_DEPLOYMENT.md)
- [DEPLOYMENT.md](./DEPLOYMENT.md)
- [README.md](./README.md)

## 💡 技术支持

- GitHub Issues: https://github.com/5181667/Aura/issues
- 邮件: (你的联系邮箱)

---

**祝部署顺利！** 🎉

有任何问题欢迎在 GitHub 提 Issue！
